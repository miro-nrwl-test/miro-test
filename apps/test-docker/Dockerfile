# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build test-docker`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t test-docker`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system test-docker && \
          adduser --system -G test-docker test-docker

COPY dist/apps/test-docker test-docker/
RUN chown -R test-docker:test-docker .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix test-docker --omit=dev -f install

CMD [ "node", "test-docker" ]
